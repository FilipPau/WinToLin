<UserControl x:Class="WinToLin.Steps.USBSetup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid Margin="20">
        <StackPanel>
            <!-- Description -->
            <TextBlock Text="Choose a USB device to create the bootable Linux installer."
                       Foreground="#A0A0A0"
                       Margin="0,0,0,15"/>

            <!-- USB List -->
            <ListBox x:Name="UsbList"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="White"
                     SelectionMode="Single">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd"
                                            Background="#1B1F28"
                                            CornerRadius="8"
                                            Padding="12">

                                        <ContentPresenter/>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#232733"/>
                                        </Trigger>

                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#2D3445"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       FontSize="15"
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Size}"
                                       Foreground="#AAAAAA"
                                       FontSize="12"/>
                            <TextBlock Text="{Binding Letter}"
                                       Foreground="#777777"
                                       FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- No USB -->
            <TextBlock x:Name="NoUsbText"
                       Text="No USB device detected. Please insert one."
                       Foreground="Gray"
                       Margin="5"
                       Visibility="Collapsed"/>

            <!-- Selected -->
            <TextBlock x:Name="SelectedUsbText"
                       Margin="5,10,0,0"
                       Foreground="#4A90E2"
                       FontWeight="Bold"/>

        </StackPanel>
    </Grid>
</UserControl>