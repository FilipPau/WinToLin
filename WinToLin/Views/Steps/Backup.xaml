<UserControl x:Class="WinToLin.Views.Steps.Backup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Modern TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Resources>
                <Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style>
            </Style.Resources>
        </Style>

        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
        </Style>

        <!-- Modern Button -->
        <Style x:Key="PrimaryAction" TargetType="Button">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Style.Resources>
                <Style TargetType="Border"><Setter Property="CornerRadius" Value="12"/></Style>
            </Style.Resources>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Lock the left column to a fixed width if star is still causing issues -->
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>

        <!-- LEFT COLUMN: SOURCES -->
        <Grid Grid.Column="0" Margin="48,40,24,40">
            <ListBox x:Name="MasterBackupList" 
                     BorderThickness="0" 
                     Background="Transparent"
                     HorizontalContentAlignment="Stretch" 
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Visible"
                     SelectionChanged="MasterBackupList_SelectionChanged">

                <ListBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource MetricLabel}" Margin="0,20,0,10" Opacity="0.7"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListBox.GroupStyle>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" CornerRadius="12" Margin="0,0,0,4" Padding="12,10" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Stretch" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#F8FAFC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#F1F5F9"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Column definitions strictly enforced -->
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/> 
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="📁" FontSize="16" VerticalAlignment="Center" Opacity="0.5"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#1E293B" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Path}" Foreground="#94A3B8" FontSize="11" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            
                            <!-- Fixed width column (40) keeps the button locked in place -->
                            <Button Grid.Column="2" Content="✕" Click="RemoveItem_Click" Tag="{Binding}" 
                                    Background="Transparent" BorderThickness="0" Foreground="#CBD5E0" 
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Width="30" Height="30" Cursor="Hand"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- RIGHT COLUMN: DETAILS -->
        <Border Grid.Column="1" Background="#F8FAFC" Padding="40">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel x:Name="DetailPanel" Visibility="Collapsed">
                    <TextBlock Text="MAPPING DETAILS" Style="{StaticResource MetricLabel}" Margin="0,0,0,30"/>
                    <TextBlock Text="SOURCE WINDOWS PATH" Style="{StaticResource MetricLabel}" FontSize="9" Opacity="0.6"/>
                    <TextBlock x:Name="SourcePathTxt" FontSize="12" Foreground="#64748B" Margin="0,8,0,25" TextWrapping="Wrap" FontWeight="Medium"/>
                    <TextBlock Text="LINUX DESTINATION" Style="{StaticResource MetricLabel}" FontSize="9" Foreground="#3182CE"/>
                    <TextBox x:Name="TargetPathInput" 
                             Style="{StaticResource ModernTextBox}" 
                             Margin="0,8,0,10"
                             TextChanged="TargetPathInput_TextChanged"/>
                </StackPanel>

                <StackPanel x:Name="EmptyDetailPlaceholder" VerticalAlignment="Center">
                    <TextBlock Text="SELECT AN ITEM" Style="{StaticResource MetricLabel}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Choose a folder to customize its path" 
                               Foreground="#94A3B8" FontSize="13" TextAlignment="Center" Margin="0,10" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Grid Margin="0,0,0,20">
                        <TextBlock Text="TOTAL SELECTED" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="TotalCountDisplay" Text="0" HorizontalAlignment="Right" FontWeight="Black" Foreground="#1E293B"/>
                    </Grid>
                    <Button Content="+ Add Custom Folder" Style="{StaticResource PrimaryAction}" Click="AddFolder_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>